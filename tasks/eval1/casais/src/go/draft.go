package main
import "fmt"
func main() {
    var animal, i, qtdanimais int
     var descasados[50]int


     fmt.Scan(animal)

    for i = 0; i < qtdanimais; i++{
        fmt.Scan(descasados)
    }


    
}
